# BitsMesh 像素级复刻设计方案

## 概述

本文档定义了将 NodeSeek 前端样式像素级复刻到 BitsMesh 主题的系统化流程。

### 目标
- 关键 CSS 属性值必须完全相同（padding、margin、字号、颜色等）
- 覆盖全部页面和组件
- 可验证、可追踪的复刻流程

### 验收标准
- 差异报告中所有关键属性差异为 0
- 通过自动化验证脚本检查

---

## 核心工作流程

### 三步复刻法

每个组件的复刻遵循固定流程：

```
┌─────────────────────────────────────────────────────────┐
│  步骤 1：提取                                            │
│  从 NodeSeek 快照提取组件的精确 CSS 属性值               │
│  → 生成「属性清单」JSON 文件                             │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 2：实现                                            │
│  Claude Code 根据属性清单编写 BitsMesh CSS               │
│  → 必须使用清单中的精确数值，禁止"差不多"                │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 3：验证                                            │
│  用 chrome-devtools 自动对比两边的计算样式               │
│  → 输出差异报告，差异项必须修复到 0                      │
└─────────────────────────────────────────────────────────┘
```

### 关键原则

1. **数据驱动**：所有样式值来自 JSON 清单，不靠目测
2. **自动验证**：每次修改后立即运行对比脚本
3. **零差异标准**：关键属性差异必须为 0 才算完成

---

## 属性清单格式

### 目录结构

```
docs/replica-specs/
├── components/
│   ├── sidebar-user-card.json
│   ├── sidebar-quick-access.json
│   ├── header-nav.json
│   ├── post-list-item.json
│   └── ...
└── pages/
    ├── homepage.json
    └── post-detail.json
```

### 单个组件清单示例

```json
{
  "component": "sidebar-user-card",
  "nodeseek_selector": ".user-card",
  "bitsmesh_selector": ".bits-user-card",
  "elements": [
    {
      "name": "container",
      "nodeseek": ".user-card",
      "bitsmesh": ".bits-user-card",
      "properties": {
        "padding": "12px 5px 5px",
        "background": "var(--bg-main-color)",
        "border-radius": "0"
      }
    },
    {
      "name": "avatar",
      "nodeseek": ".avatar-normal",
      "bitsmesh": ".bits-avatar-normal",
      "properties": {
        "width": "40px",
        "height": "40px",
        "border-radius": "15%",
        "box-shadow": "0 0 3px rgba(0,0,0,.1)"
      }
    }
  ]
}
```

### 关键属性范围

| 类别 | 属性 |
|-----|-----|
| 尺寸 | width, height, padding, margin |
| 布局 | display, flex, gap, align-items, justify-content |
| 文字 | font-size, font-weight, line-height, color |
| 装饰 | background, border, border-radius, box-shadow |

---

## 自动验证机制

### 验证流程

```
┌──────────────────┐     ┌──────────────────┐
│   NodeSeek 页面   │     │   BitsMesh 页面   │
│   (本地快照)      │     │   (localhost)     │
└────────┬─────────┘     └────────┬─────────┘
         │                        │
         ▼                        ▼
    getComputedStyle()      getComputedStyle()
         │                        │
         └──────────┬─────────────┘
                    ▼
            ┌──────────────┐
            │   对比引擎    │
            │  逐属性比较   │
            └──────┬───────┘
                   ▼
            ┌──────────────┐
            │   差异报告    │
            └──────────────┘
```

### 差异报告格式

```
═══════════════════════════════════════════════════
  组件: sidebar-user-card    状态: ❌ 3 项差异
═══════════════════════════════════════════════════

  元素: .bits-avatar-normal
  ┌─────────────┬────────────┬────────────┬────────┐
  │ 属性        │ NodeSeek   │ BitsMesh   │ 状态   │
  ├─────────────┼────────────┼────────────┼────────┤
  │ width       │ 40px       │ 40px       │ ✅     │
  │ height      │ 40px       │ 42px       │ ❌     │
  │ border-radius│ 15%       │ 15%        │ ✅     │
  └─────────────┴────────────┴────────────┴────────┘

═══════════════════════════════════════════════════
  总计: 12 项检查, 9 通过, 3 失败
═══════════════════════════════════════════════════
```

---

## 组件清单

### 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│                        HEADER                                │
│  ┌─────────┐ ┌──────────────────────┐ ┌──────────────────┐  │
│  │  Logo   │ │     Nav Tabs         │ │  Search + User   │  │
│  └─────────┘ └──────────────────────┘ └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘

┌───────────────────────────────────┐ ┌───────────────────────┐
│            MAIN CONTENT           │ │       SIDEBAR         │
│  ┌─────────────────────────────┐  │ │ ┌───────────────────┐ │
│  │      Sort Tabs (新/热)      │  │ │ │    User Card      │ │
│  └─────────────────────────────┘  │ │ └───────────────────┘ │
│  ┌─────────────────────────────┐  │ │ ┌───────────────────┐ │
│  │      Post List Item         │  │ │ │  New Post Button  │ │
│  └─────────────────────────────┘  │ │ └───────────────────┘ │
│  ┌─────────────────────────────┐  │ │ ┌───────────────────┐ │
│  │        Pagination           │  │ │ │   Quick Access    │ │
│  └─────────────────────────────┘  │ │ └───────────────────┘ │
└───────────────────────────────────┘ │ ┌───────────────────┐ │
                                      │ │   Category List   │ │
                                      │ └───────────────────┘ │
                                      │ ┌───────────────────┐ │
                                      │ │  Community Stats  │ │
                                      │ └───────────────────┘ │
                                      └───────────────────────┘
```

### 完整组件列表

| 序号 | 组件 | 优先级 | 状态 |
|-----|------|-------|------|
| **Header** ||||
| 1 | header-container | P0 | 🔄 待验证 |
| 2 | header-logo | P0 | 🔄 待验证 |
| 3 | header-nav-tabs | P0 | 🔄 待验证 |
| 4 | header-search | P1 | 🔄 待验证 |
| 5 | header-user-menu | P1 | 🔄 待验证 |
| **Sidebar** ||||
| 6 | sidebar-user-card | P0 | 🔄 待验证 |
| 7 | sidebar-new-post-btn | P0 | 🔄 待验证 |
| 8 | sidebar-site-info | P1 | 🔄 待验证 |
| 9 | sidebar-quick-access | P0 | 🔄 待验证 |
| 10 | sidebar-category-list | P1 | 🔄 待验证 |
| 11 | sidebar-community-stats | P2 | 🔄 待验证 |
| **Main Content** ||||
| 12 | sort-tabs | P0 | 🔄 待验证 |
| 13 | post-list-item | P0 | 🔄 待验证 |
| 14 | pagination | P1 | 🔄 待验证 |
| **Post Detail** ||||
| 15 | post-content | P0 | 🔄 待验证 |
| 16 | comment-item | P0 | 🔄 待验证 |
| 17 | comment-editor | P1 | 🔄 待验证 |
| **Footer** ||||
| 18 | footer | P2 | 🔄 待验证 |

**优先级说明：**
- **P0** = 核心组件，优先复刻
- **P1** = 重要组件
- **P2** = 次要组件

---

## Claude Code 执行指令

### 指令列表

| 指令 | 功能 |
|-----|-----|
| `/replica-extract <component>` | 从 NodeSeek 提取属性清单 |
| `/replica-implement <component>` | 根据清单实现 BitsMesh CSS |
| `/replica-verify <component>` | 对比验证，输出差异报告 |
| `/replica-fix <component>` | 自动修复差异项 |
| `/replica-status` | 查看所有组件复刻进度 |

### 单个组件执行流程

```
用户: /replica-extract sidebar-user-card

Claude Code:
  1. 打开 NodeSeek 快照页面 (chrome-devtools)
  2. 定位 .user-card 及其子元素
  3. 提取所有关键 CSS 属性的 computedStyle
  4. 生成 docs/replica-specs/components/sidebar-user-card.json
  5. 输出提取报告

用户: /replica-implement sidebar-user-card

Claude Code:
  1. 读取属性清单 JSON
  2. 对照 BitsMesh 现有 CSS
  3. 逐属性写入/修正，使用清单中的精确值
  4. 保存文件

用户: /replica-verify sidebar-user-card

Claude Code:
  1. 同时打开 NodeSeek 快照 + BitsMesh localhost
  2. 对比每个元素的 computedStyle
  3. 输出差异报告表格
  4. 差异项 > 0 则标记为 ❌ 未通过

用户: /replica-fix sidebar-user-card

Claude Code:
  1. 读取差异报告
  2. 自动修正 CSS 中的差异值
  3. 重新验证直到差异为 0
  4. 输出 ✅ 通过
```

---

## 强制规则

执行任何复刻任务时，必须遵守：

1. **禁止凭感觉写 CSS** — 所有数值必须来自属性清单
2. **禁止"差不多"** — 差异报告必须为 0 才算完成
3. **先提取后实现** — 没有属性清单不允许写 CSS
4. **每次修改必验证** — 改完立即运行 verify
5. **变量映射表** — NodeSeek 的 CSS 变量对应 BitsMesh 变量：
   - `--bg-main-color` → `--bits-bg-main`
   - `--link-color` → `--bits-link-color`
   - `--text-color` → `--bits-text-color`

---

## 参考资源

- NodeSeek 快照目录：`nodeseek-web-snap/`
- NodeSeek CSS：`nodeseek-web-snap/用户登录时首页/NodeSeek_files/nsk-style.*.css`
- BitsMesh 主题目录：`themes/bitsmesh/`
- BitsMesh CSS：`themes/bitsmesh/design/bits-sidebar.css`

---

## 创建时间

2026-01-21
