# Theme Boilerplate 主题文档

[根目录](../../CLAUDE.md) > [themes](../) > **theme-boilerplate**

---

## 模块职责
**Theme Boilerplate** 是 Vanilla 响应式主题的 SCSS 开发模板，供主题开发者快速开始定制主题。

---

## 核心特性

### 1. 现代开发工具链
- **SCSS**：模块化样式编写
- **Webpack/Yarn**：前端构建
- **ES6+ JavaScript**：现代 JS 语法

### 2. 响应式设计
- 移动优先（Mobile-First）
- 自适应布局
- 断点控制

### 3. 开发友好
- 热重载（Hot Reload）
- Source Maps
- 开发/生产模式分离

---

## 文件结构
```
themes/theme-boilerplate/
├── addon.json                          # 主题元数据
├── package.json                        # NPM 依赖（推测）
├── yarn.lock                           # Yarn 锁文件
├── settings/configuration.php          # 主题配置
├── design/
│   ├── custom.css                      # 编译后的 CSS
│   ├── custom.css.map                  # Source Map
│   ├── fonts/kbicons/                  # 图标字体
│   └── images/                         # SVG 图标
├── js/
│   ├── custom.js                       # 编译后的 JS
│   └── custom.js.map                   # Source Map
├── src/
│   └── js/
│       ├── index.js                    # JS 入口
│       ├── utility.js                  # 工具函数
│       └── mobileNavigation.js         # 移动端导航
├── views/
│   ├── default.master.tpl              # 主模板
│   └── partials/                       # 模板片段
│       ├── header.tpl
│       ├── footer.tpl
│       └── hamburger.html
├── setup/
│   ├── boilerplateInstall.js           # 安装脚本
│   ├── utils.js                        # 工具函数
│   └── src/index.js                    # 脚手架脚本
├── screenshot.png                      # 桌面预览图
└── mobile.png                          # 移动端预览图
```

---

## 开发流程

### 1. 安装依赖
```bash
cd themes/theme-boilerplate
yarn install
```

### 2. 开发模式
```bash
yarn dev    # 启动热重载
```

### 3. 生产构建
```bash
yarn build  # 编译压缩资源
```

---

## 使用场景
- 创建全新主题
- 学习主题开发
- 定制企业主题
- 响应式设计实践

---

## 常见问题

### Q1: 如何基于 Boilerplate 创建主题？
1. 复制整个目录并重命名
2. 修改 `addon.json` 中的主题信息
3. 运行 `yarn install`
4. 开始开发

### Q2: 需要懂 SCSS 吗？
推荐学习 SCSS，但也可以直接修改编译后的 CSS。

### Q3: 图标字体如何使用？
使用 `kbicons` 字体族，具体图标代码参考字体文件。

### Q4: 支持哪些浏览器？
取决于构建配置，通常支持现代浏览器和 IE11+。

---

## 相关资源
- [Vanilla 主题文档](https://docs.vanillaforums.com/developer/theming/)
- [SCSS 指南](https://sass-lang.com/guide)

---

**最后更新**：2026-01-17 20:57:17 | **版本**：1.0.0 | **状态**：开发模板 🛠️ | **许可证**：GPL-2.0-only
